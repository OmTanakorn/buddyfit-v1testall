# Direct dependencies only. Transitive packages are resolved by pip.
# Install with: pip install -r requirements.txt

Django~=5.2.0                 # LTS, security support until April 2028
django-allauth==65.19.0
django-crispy-forms==2.7
crispy-bootstrap5==2026.3
django-cors-headers==4.9.0
whitenoise==6.12.0

# Production only
gunicorn==26.0.0
psycopg[binary]==3.3.4        # used when DATABASE_URL is set

# Development only
django-debug-toolbar==7.1.0   # loaded solely when DJANGO_DEBUG is on
